import React from "react"

const Header = () => {
// function Header () {
    return (
        <header>
            <h1>Jang's To Do</h1>
            <p>Enter your item(s) via input field</p>
        </header>
    )
}

export default Header